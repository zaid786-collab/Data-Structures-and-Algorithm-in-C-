#include<bits/stdc++.h>
using namespace std;
// Constant time complexity is one of the best time complexity.

// For 10^9 inputs the operations will be 10^9 in case of linear T.C but in constant T.C it will only be 1 or n(3,8,9,11...).
int main(){

    return 0;
}
// Constant Time Complexity[O(1)] means numbers of operations remain constant doesn't matter how much input size(n) increses.
// For ex : to print smallest number in array which is sorted in incresing order so number of opeartion is 1 or constant[3,6,11..] which is to print 0th index of array.