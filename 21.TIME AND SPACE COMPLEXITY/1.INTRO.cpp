#include<bits/stdc++.h>
using namespace std;
// Time complexity means the praportion in which number of operations increses with increse in input size(n).
// If number of op increses linearly with wrt to input size(n) the it is linera TC if operations increses double the it is (n^2) T.C.
// We ignore constants while calculating Time Complexity.
int main(){

    return 0;
}
// Time Complexity is NOT actual time taken to run a program for a same code actual time can be different depending on server and programming language but Time complexity remains same.

// Time Complexity is a relation btw input size(n) and time take wrt it,it means with increse in input size(n) how much time changes with increse in operations time increses or decreses.