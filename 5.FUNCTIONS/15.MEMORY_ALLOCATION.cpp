#include<iostream>
using namespace std;
int main(){
    /* 
    1)  Memory is stored in two ways,one is stack and another one is heaps,functions are stored in the form of stack which is like one over another,whenever new function is accessed(compiler position) then new stack frame is created where all the content and synatx related to that function is stored.
    2)  As soon as the value is returned or return statement is excecuted then that stack frame will get completely erased from the memory.
    3)  Every time function which is under an excecution will remain at the top of the stack while remainig frame will remain below to that stack frame.
    4)  Current function is at the top of the stack.
    */
    return 0;
}