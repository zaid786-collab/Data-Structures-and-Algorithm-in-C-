#include<bits/stdc++.h>
using namespace std;

int main(){

    return 0;
}
/*
1. Every time a recursive call takes place[means a function call itself] then a new stack frame is created in a stack.

2. Until a base condition is not reached values will not return,after condition is hit then values will return in reverse recursive order.

3. As the value is returned,then satck frame of that function will be deleted from a stack.
*/