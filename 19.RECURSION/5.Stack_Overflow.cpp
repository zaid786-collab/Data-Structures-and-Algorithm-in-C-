#include<bits/stdc++.h>
using namespace std;

int main(){

    return 0;
}
/*
1. Stack overflow occurs when memory in stack sorts,this happen because of infinite recursion.

2. Two reasons for infinite recursion is when we declared so many local variables[arrray's] in a function and when base case is missing[which will result in infinite recursive calls and infinite stack frames resulting in stack overflow] and causing segmentation fault.
*/