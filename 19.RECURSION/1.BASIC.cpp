#include<bits/stdc++.h>
using namespace std;

int main(){

    return 0;
}
/*
1. Recursion is method when function call itself repetedly,it is similar to itarative loops.

2. Base Case[End Condition] is required in recursion to end the recursion after certain condition is reached.

3. Recursion is used when we have to break a large Junk of Problem into smaller segments and solve each segement via a function.

4. It is a method of solving computational problems where the solution depends on solutions to smaller instances of the same problem.
*/