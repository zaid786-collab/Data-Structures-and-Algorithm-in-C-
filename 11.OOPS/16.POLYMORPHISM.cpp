#include<bits/stdc++.h>
using namespace std;
int main(){ 
    /*
    1. Polymorphism is the ability of object to take on different forms or behave in different ways depending  on the context in whcih they are used.

    2. Compile Time Polymorphism --> Which function to use as per the condition  is chosen at compile time,compile time Polymorphism is further divided into Opearator Overloding or Function Overloding.

    3. Run Time Polymorphism --> Which function to use as per the condition is chosen at runtime,it is divided into Function Overriding and Virtual Functions.

    4. Overloding is Compile Time Polymorphism and Overriding is Run Time Polymorphism.

    5. In Overloding there is a function with same name in same class,in overriding there is a function of same name in Parent and Child Class.
    */
    return 0;
}